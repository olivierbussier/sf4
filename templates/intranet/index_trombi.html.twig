{% extends 'base.html.twig' %}

{% block title %}Trombinoscope{% endblock %}

{% block main_page %}

    <div class="container">
        <h3>Trombinoscope</h3>
        <div class="col-xs-12">
            <a class="btn btn-default" href="{{ url('index_intranet') }}">
                <span class="glyphicon glyphicon-chevron-left"></span> Revenir à l'accueil de l'intranet.
            </a>
        </div>

        <div class="col-xs-12">

        {% for photo in photos %}
            {{ dump(photos) }}
        {% endfor %}

            if ($data['FTROMBI'] == "NON") {
            $fname = "../images/notrombi.jpg";
            } else {
            $fname = PhotoHelpers::corrigerPath(
            "../identites/".$data['NOM']."-".$data['PRENOM']."-".$data['Ref']."-th.jpg"
            );
            }
            ?>              <div class="case-trombi">
                <?php
if (file_exists($fname)) { ?>
                <img src="<?= $fname; ?>" class="img-trombi">
                <?php
} else { ?>
                <img src="../images/default.jpg" class="img-trombi">
                <?php
} ?>
                <div class ="nomblu">
                    <center><?= $data['NOM'].'<br>'.$data['PRENOM'].'<br>'.$data['NIVEAU']; ?></center>
                </div>
            </div>
            <?php
}

?>          </div>
        <div class="col-xs-12">
            <a  class="btn btn-default" href="index_intranet.php">
                <span class="glyphicon glyphicon-chevron-left"></span> Revenir à l'accueil de l'intranet.</a>
        </div>
    </div>
    <?php $html->end();
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Trombinoscope{% endblock %}

{% block main_page %}

    <div class="container">
        <h3>Gestion du calendrier des activités</h3>

        <div class="col-xs-12">
            <a class="btn btn-light btn-outline-guc" href="{{ url('index_intranet') }}">
                <span class="glyphicon glyphicon-chevron-left"></span> Revenir à l'accueil de l'intranet.
            </a>
        </div>

        <div class="col-xs-12">
        </div>

        <div class="col-xs-12">
            <a  class="btn btn-light btn-outline-guc" href="{{ url('root') }}">
                <span class="glyphicon glyphicon-chevron-left"></span> Revenir à l'accueil de l'intranet.
            </a>
        </div>

    </div>

{% endblock %}
